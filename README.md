# Система Учета Заявок для Сервисного Центра

![Python](https://img.shields.io/badge/python-3.11-blue.svg) ![PyQt6](https://img.shields.io/badge/PyQt-6-green.svg) ![PostgreSQL](https://img.shields.io/badge/PostgreSQL-14-blue.svg)

Десктопное приложение для автоматизации работы сервисного центра по ремонту климатической техники. Разработано на Python с использованием фреймворка PyQt6 и СУБД PostgreSQL.

## 🚀 Основной функционал

- **Авторизация и Регистрация:** Безопасный вход в систему с хэшированием паролей (`bcrypt`).
- **Ролевая модель доступа:**
  - **Заказчик:** Может создавать заявки, просматривать их статусы и оставлять комментарии.
  - **Мастер:** Может просматривать назначенные заявки, изменять их статусы и добавлять комментарии о ходе ремонта.
  - **Администратор:** Управляет пользователями (назначение ролей), просматривает всю статистику и имеет доступ ко всем заявкам.
- **Управление заявками:** Создание, просмотр, редактирование и фильтрация заявок.
- **Статистика и Аналитика:** Встроенная панель для администратора с ключевыми метриками (количество заявок, статусы, среднее время выполнения).
- **Импорт данных:** Скрипт для первоначального заполнения базы данных из CSV-файлов.
- **Генерация QR-кодов:** Возможность сгенерировать QR-код со ссылкой на форму обратной связи.

## 🛠️ Технологический стек

- **Язык программирования:** Python 3.11
- **Графический интерфейс:** PyQt6
- **База данных:** PostgreSQL
- **Библиотеки:** `psycopg2-binary`, `bcrypt`, `qrcode`

## ⚙️ Установка и запуск

1.  **Клонируйте репозиторий:**
    ```bash
    git clone https://github.com/MrBombastick08/Education-practice-2.git
    cd Education-practice-2
    ```

2.  **Установите зависимости:**
    ```bash
    pip install -r requirements.txt
    ```
    *(Примечание: если файла `requirements.txt` нет, установите пакеты вручную)*
    ```bash
    pip install psycopg2-binary bcrypt PyQt6 qrcode
    ```

3.  **Настройте базу данных:**
    - Убедитесь, что у вас установлен и запущен **PostgreSQL**.
    - Создайте базу данных с именем `climate_service`.
    - Выполните SQL-скрипт `table.sql` для создания таблиц.

4.  **Настройте подключение к БД:**
    - Откройте файл `database_module.py`.
    - В классе `Database` укажите ваши параметры подключения (host, user, password, port).

5.  **Импортируйте начальные данные (опционально):**
    - Убедитесь, что файлы `inputDataUsers.csv`, `inputDataRequests.csv` и `inputDataComments.csv` находятся в корневой папке.
    - Запустите скрипт `import_data.py`:
    ```bash
    python import_data.py
    ```

6.  **Запустите приложение:**
    ```bash
    python main_app.py
    ```

## 📂 Структура проекта

| Файл/Папка | Описание |
| :--- | :--- |
| `main_app.py` | Основной файл приложения с логикой UI. |
| `database_module.py` | Модуль для взаимодействия с базой данных PostgreSQL. |
| `import_data.py` | Скрипт для импорта данных из CSV в БД. |
| `qr_generator.py` | Модуль для генерации QR-кодов. |
| `table.sql` | SQL-скрипт для создания структуры таблиц. |
| `*.csv` | Файлы с исходными данными для импорта. |
